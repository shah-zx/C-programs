#include<stdio.h>
#include<conio.h>

int main()

{
	int i ,c=0, arr[10];
	char a;
	puts("Enter the string ");
	gets (arr);
	puts("Enter the character to be found:- ");
	a = getchar();
	for(i=0;i<10;i++)
	{
		if(arr[]!==a)
		
	}
}
