// here we will see how can we pass arrays as arguments to function :-

// We pass arrays to a afunction when we need to pass a set of values to a given function,.
// We can pass the array as a parameter in the function.
// By declaring the array as a parameter function.
// By declaring a pointer to the function to hold the base address of the array.


#include<stdio.h>
#include<conio.h>

int main()


{





}